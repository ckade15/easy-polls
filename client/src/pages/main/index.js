import React from "react";
import Header from "../../common/header";

const Main = (props) => {
    return (
        <section>
            <Header />

        </section>
    );
}

export default Main;